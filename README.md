# Raspberry Pi Kernel Builder

This project provides a means to easily compile a custom RaspberryPi Kernel. It
is fully docker enabled. With a few simple config variables everybody knowing
how to use docker can compile a new RaspberryPi Kernel.

...